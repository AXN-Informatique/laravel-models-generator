<?php
/*
 * NOTE IMPORTANTE !!
 *
 * Ce fichier a été généré par la commande "artisan models:generate"
 * (package axn/models-generator)
 *
 * Vous pouvez y faire des modifications, sauf entre les tags qui entourent les
 * relations, celles-ci étant raffraîchies à chaque lancement de la commande de
 * génération des modèles.
 */

namespace {{namespace}};

use Illuminate\Database\Eloquent\Model;

class {{name}} extends Model
{
    /**
     * Nom de la table qui correspond à ce modèle.
     *
     * @var string
     */
    protected $table = '{{tableName}}';

    /**
     * Liste des champs protégés contre l'assignation/modification.
     *
     * @var array
     */
    protected $guarded = ['id'];

{{relations}}
}
